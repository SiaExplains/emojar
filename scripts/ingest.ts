/**
 * Placeholder: Ingest Unicode CLDR / Emoji data to build `data/emojis.json`.
 * Steps you will implement later:
 * 1) Download CLDR annotations + emoji-test.txt from Unicode.
 * 2) Parse codepoints, names, annotations (keywords), categories.
 * 3) Slugify names and write to `data/emojis.json`.
 * Keep file small and sorted for fast client load.
 */
console.log('TODO: implement ingest pipeline');
